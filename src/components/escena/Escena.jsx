// Lo exportamos con nombre (mismo que el archivo), no por default
// Al contener un único <tag> o elemento, no hace falta envolverlo en un contenedor padre
export const Escena = () => {
    return <p>El nostre heroi estava surant per l'espai sideral quan a la llunyania va albirar una nau espacial</p>;
};
